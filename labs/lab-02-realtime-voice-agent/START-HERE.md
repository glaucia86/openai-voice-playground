# Comece aqui — Lab 02

Esta branch é o ponto de partida executável do workshop de agente de voz Realtime.

1. Abra o terminal nesta pasta.
2. Execute `npm ci`.
3. Copie `.env.example` para `.env.local` e preencha `OPENAI_API_KEY` somente quando o tutorial solicitar.
4. Execute `npm run dev` e abra `http://localhost:3000`.
5. Siga o [tutorial em português](tutorial/tutorial.md), começando pelo caminho **B — starter recomendado**.

A base deve passar em `npm run check` sem solicitar microfone e sem abrir uma sessão Realtime. Consulte os checkpoints com `git show` ou `git diff`; não troque de branch em cima do seu trabalho sem antes criar seu próprio branch.

[Read this file in English](START-HERE-EN.md)
